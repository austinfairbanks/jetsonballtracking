# Copy to ops/.local/jetson.env. This example contains no real connection details.
# "jetson" is an alias you configure in your own ~/.ssh/config.
JETSON_SSH_TARGET="${JETSON_SSH_TARGET:-jetson}"
JETSON_REMOTE_DIR="${JETSON_REMOTE_DIR:-code/jetsonballtracking}"
